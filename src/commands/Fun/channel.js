import { SlashCommandBuilder } from 'discord.js'; export default { data: new SlashCommandBuilder() .setName('channel') .setDescription('Test des Channel Commands'), category: 'Utility', async execute(interaction, config, client) { await interaction.reply({ content: '🟢 **TEST ERFOLGREICH!**\n\nDie neue `channel.js` wird geladen.', ephemeral: false, }); }, };
